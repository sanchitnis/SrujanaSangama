# Rename OpenClaw to AI Agent Proposal

## Problem

The term "OpenClaw" is used throughout the `personal-productivity` domain markdown files. It should be replaced with "AI agent" to align with updated naming conventions.

## Proposed Change

Replace case-sensitive occurrences of the term "OpenClaw" with "AI agent" in all `.md` files in the `domains/personal-productivity` directory.

## Scope Boundaries

### In Scope
- All `.md` files under `domains/personal-productivity/` containing "OpenClaw".

### Out of Scope
- Config files like YAML files or other non-markdown files.
- Occurrences of "openclaw" in lowercase (e.g., in scripts or keys).

## Verification
- Search for "OpenClaw" in `domains/personal-productivity/` and confirm no case-sensitive matches remain.
